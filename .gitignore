env/
__pycache__*
lightning_logs/
legacy/
outputs/
env_cop/
.vscode/
*.ipynb
*.ckpt
*.csv
*.json
*.png
*.pt
multirun/
results/
!eda_legacy_outcomes.ipynb
!eda_survival_intervals.ipynb
!eda_joint_tokens.ipynb

# Ignore everything under data/ EXCEPT the SMARTEHR data dictionaries
# (schema + code maps; no patient records), so the enrichment mapping ships with
# the repo. Nested negation: re-include each parent dir, then the *.csv files
# (the last rule must come after the global *.csv ignore above to win).
data/*
!data/smartehr/
data/smartehr/*
!data/smartehr/data_dicts/
data/smartehr/data_dicts/*
!data/smartehr/data_dicts/*.csv
